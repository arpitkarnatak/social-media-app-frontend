import { styled } from "styled-components";

export const MainHeaderContainer = styled.header`
  width: 100%;
  padding: 12px 3%;
  position: sticky;
  top: 0;
  display: flex;
  justify-content: space-between;
  flex-wrap: wrap;
  border: 2px solid #b8b8b8;
  background: linear-gradient(90deg, rgba(0,0,123,1) 0%, rgba(16,133,210,1) 100%);
`;
